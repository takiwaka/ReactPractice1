import React from "react";

const Input = () => {
  return (
    <div>
      <input type="text" placeholder="TODO入力" />
      <button>追加</button>
    </div>
  );
};

export default Input;
